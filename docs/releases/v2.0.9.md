# Awake 2.0.9

Manual Awake sessions, timers, and `awake run` now keep the battery safeguard active even if the optional agent daemon is off.

Awake forces sleep when battery reaches the configured critical floor. The default floor is 5%.

If Awake cannot start the monitor, it refuses to keep the session awake.
